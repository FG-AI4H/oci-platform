{
  "name": "@oci/migrate",
  "version": "0.0.0",
  "private": true,
  "description": "One-shot container that runs prisma migrate deploy against the environment's Aurora cluster, then seeds demo data on non-prod.",
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1048.0",
    "prisma": "^7.8.0"
  },
  "overrides": {
    "picomatch": "^4.0.4",
    "deepmerge-ts": "^8.0.0"
  }
}
