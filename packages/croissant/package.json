{
  "name": "@oci/croissant",
  "version": "0.0.0",
  "private": true,
  "description": "BIOCroissant \u2014 Healthcare extension to MLCommons Croissant. Validators, generators, ingestion helpers.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint stub \u2014 Phase A1 will add eslint config\" && exit 0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "ajv": "^8.17.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
